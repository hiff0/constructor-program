version: '3.9'

volumes:
  node_modules:

services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile.dev
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    volumes:
      - .:/src
      - node_modules:/src/node_modules